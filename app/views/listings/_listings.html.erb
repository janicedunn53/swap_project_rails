<ul id="listing_items">
  <h4>Click on an item for more details!</h4>
  <% @listingable.listings.each do |listing| %>
    <li>
      <h4><%= link_to listing.name, listing_path(listing) %></h4>
      <%= listing.description%>
    </li>
  <% end %>
</ul>
