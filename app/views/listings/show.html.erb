<h1><%= @listing.name %></h1>
<p><%= @listing.description %></p>

<p><%= link_to "Edit Listing", edit_listing_path(@listing) %></p>
<p><%= link_to "Delete Listing", listing_path(@listing), :data => {:confirm => "Are you sure you want to delete this listing?", :method => "delete"} %></p>

<hr>
<p><%= link_to "Home", listings_path %></p>
